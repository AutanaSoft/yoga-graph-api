generator client {
  provider            = "prisma-client"
  output              = "./generated"
  moduleFormat        = "cjs"
  importFileExtension = "js"
  binaryTargets       = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
}
